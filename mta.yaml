_schema-version: "3.2"
ID: comsapacp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comsapacp-destination-content
  type: com.sap.application.content
  requires:
  - name: comsapacp-destination-service
    parameters:
      content-target: true
  - name: comsapacp-repo-host
    parameters:
      service-key:
        name: comsapacp-repo-host-key
  - name: comsapacp-uaa
    parameters:
      service-key:
        name: comsapacp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comsapacp_html_repo_host
          ServiceInstanceName: comsapacp-html5-srv
          ServiceKeyName: comsapacp-repo-host-key
          sap.cloud.service: comsapacp
        - Authentication: OAuth2UserTokenExchange
          Name: comsapacp_uaa
          ServiceInstanceName: comsapacp-xsuaa-srv
          ServiceKeyName: comsapacp-uaa-key
          sap.cloud.service: comsapacp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comsapacp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comsapacp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsapacp.zip
      name: comsapacp
      target-path: resources/
- name: comsapacp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comsapacp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comsapacp-destination-service
    service-plan: lite
- name: comsapacp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comsapacp-xsuaa-srv
    service-plan: application
- name: comsapacp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comsapacp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
